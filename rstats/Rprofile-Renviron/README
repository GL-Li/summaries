Two hidden files .Rprofile and .Renviron are in this directory.

To test the files, start R from this directory and then check:
- Sys.getenv("AAA") to get environment variables.
- x to show the loaded object x in .Rprofile
